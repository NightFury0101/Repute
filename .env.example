# Database connection. Default is a local SQLite file — zero setup required.
# To switch to PostgreSQL for production, change the datasource provider in
# prisma/schema.prisma to "postgresql", swap the driver adapter in src/lib/db.ts
# to @prisma/adapter-pg, and set this to a postgres:// connection string.
DATABASE_URL="file:./dev.db"

# Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-random-secret"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Used once by `npm run db:seed` to create the initial admin account.
ADMIN_SEED_EMAIL="admin@maldibay.com"
ADMIN_SEED_PASSWORD="change-me-before-seeding"

# Used once by `npm run db:seed` to create a demo customer account.
CUSTOMER_SEED_EMAIL="customer@maldibay.com"
CUSTOMER_SEED_PASSWORD="change-me-before-seeding"

# --- Optional: real payment provider (not yet wired up, see checkout flow) ---
# STRIPE_SECRET_KEY=""
# STRIPE_WEBHOOK_SECRET=""
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
